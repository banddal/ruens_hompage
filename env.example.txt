# 이 파일을 복사해서 .env.local 을 만들고 실제 값을 채우세요.
#   cp .env.example .env.local
# .env.local 은 .gitignore 에 의해 커밋되지 않습니다.
# 운영 환경(Vercel)에서는 이 값들을 대시보드 Environment Variables 에 등록합니다.

# Supabase 프로젝트 URL (공개돼도 무방)
SUPABASE_URL=https://lzwkhsbfpachkaudszjo.supabase.co

# Supabase service_role 키 — 🔴 절대 프론트 코드/깃에 넣지 말 것
SUPABASE_SERVICE_ROLE_KEY=

# Storage 버킷 이름
SUPABASE_STORAGE_BUCKET=portfolio-assets

# 관리자 로그인 비밀번호 (최소 10자)
ADMIN_PASSWORD=

# 세션 서명용 긴 랜덤 문자열 (미설정 시 ADMIN_PASSWORD 로 대체됨)
ADMIN_SECRET=
